# AKM Spring Boot 3 开发规范

## 一、代码规范

### 1.1 命名规范

#### 包命名
```
com.akm.springboot3.core      - 核心模块
com.akm.springboot3.web.biz   - 业务模块
com.akm.springboot3.web.sys   - 系统模块
com.akm.springboot3.file      - 文件模块
```

#### 类命名
- **Controller**: `XxxApi` (不使用 Controller 后缀)
- **Service**: `XxxService` + `XxxServiceImpl`
- **Mapper**: `XxxMapper`
- **Entity**: `BizXxx` / `SysXxx` (带业务前缀)
- **DTO/VO**: 具体业务名称，如 `MessageQuery`, `AppCheckUpdate`

#### API 路径命名
```java
/xxx/view/{method}    // 查询接口
/xxx/op/{method}      // 操作接口（新增、编辑、删除）
/xxx/open/{method}    // 开放接口（不需要权限）
/xxx/public/{method}  // 公共接口（仅验证登录）
```

### 1.2 注解使用

#### Swagger 文档注解
```java
@Tag(name = "用户管理")
@Operation(summary = "查询用户列表", description = "支持分页和条件查询")
@Parameter(name = "id", description = "用户ID", required = true)
```

#### 自定义注解
- `@ApiFreqLimit`: 接口频率限制
- `@CheckCsrfToken`: CSRF Token 校验
- `@IgnoreResultHandler`: 忽略统一响应封装
- `@IgnoreSysLog`: 忽略系统日志记录

### 1.3 日期处理
```java
// 使用 java.util.Date (不使用 java.time)
@JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "GMT+8")
private Date createTime;
```

## 二、分层架构

### 2.1 控制器层 (Api)

```java
@Tag(name = "用户管理")
@RestController
@RequestMapping("/sys/user")
public class SysUserApi {

    private final SysUserService userService;

    // 使用构造器注入，不使用 @Autowired
    public SysUserApi(SysUserService userService) {
        this.userService = userService;
    }

    @Operation(summary = "查询列表")
    @GetMapping("/view/list")
    public PageResult<SysUser> list(SysUserQuery query) {
        return userService.queryList(query);
    }

    @Operation(summary = "新增用户")
    @PostMapping("/op/add")
    public void add(@RequestBody @Valid SysUser user) {
        userService.add(user);
    }
}
```

### 2.2 服务层 (Service)

```java
public interface SysUserService {
    PageResult<SysUser> queryList(SysUserQuery query);
    void add(SysUser user);
}

@Service
public class SysUserServiceImpl implements SysUserService {
    
    private final SysUserMapper userMapper;
    
    public SysUserServiceImpl(SysUserMapper userMapper) {
        this.userMapper = userMapper;
    }
    
    @Override
    public PageResult<SysUser> queryList(SysUserQuery query) {
        PageHelper.startPage(query.getPage(), query.getSize());
        List<SysUser> list = userMapper.findByAll(query);
        return PageResult.of(list);
    }
}
```

### 2.3 数据访问层 (Mapper)

```java
@Mapper
public interface SysUserMapper {
    List<SysUser> findByAll(SysUserQuery query);
    int insertSelective(SysUser record);
    int updateByPrimaryKeySelective(SysUser record);
    int deleteByPrimaryKey(String id);
}
```

## 三、数据库规范

### 3.1 MyBatis XML

```xml
<!-- 使用 #{param} 防止 SQL 注入 -->
<select id="findByAll" resultType="com.akm.springboot3.web.sys.entity.SysUser">
    SELECT * FROM sys_user
    WHERE del_flag = '0'
    <if test="username != null and username != ''">
        AND username LIKE CONCAT('%', #{username}, '%')
    </if>
</select>

<!-- 打印 SQL -->
<!-- akm.mybatis.print-sql: true -->
```

### 3.2 分页查询

```java
// Service 层
PageHelper.startPage(page, size);
List<SysUser> list = userMapper.findByAll(query);
return PageResult.of(list);
```

### 3.3 实体类

```java
@Getter
@Setter
@ToString
public class SysUser {
    private String id;
    private String username;
    
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "GMT+8")
    private Date createTime;
}
```

## 四、缓存规范

### 4.1 缓存 Key 管理

```java
// 统一在 RedisKeys 常量类中定义
public class RedisKeys {
    public static final String PREFIX = "akm:";
    public static final String USER_INFO = "user:info:";
    public static final String TOKEN = "token:";
}

// 使用 CacheUtils.prefixKey() 添加前缀
String key = CacheUtils.prefixKey(RedisKeys.USER_INFO + userId);
```

### 4.2 缓存操作

```java
// 对象缓存
CacheUtils.set("user:1", userObject, 3600);
User user = CacheUtils.get("user:1");

// 字符串缓存
StringCacheUtils.set("token", "value", 1800);
String token = StringCacheUtils.get("token");

// 防止缓存击穿
User user = CacheUtils.cacheAndGet("user:1", 3600, () -> {
    return userMapper.selectByPrimaryKey("1");
});
```

## 五、异常处理

### 5.1 业务异常

```java
// 抛出业务异常
throw new BusinessException(CodeMsg.USER_NOT_FOUND);
throw new BusinessException("用户不存在");

// 断言工具
AssertUtils.notNull(user, "用户不存在");
AssertUtils.isTrue(user.isEnabled(), "用户已禁用");
```

### 5.2 全局异常处理

系统已配置 `GlobalExceptionHandler` 统一处理异常，无需手动 try-catch。

## 六、参数校验

### 6.1 使用 Validation

```java
public class UserAddDTO {
    @NotBlank(message = "用户名不能为空")
    @Size(min = 3, max = 20, message = "用户名长度3-20个字符")
    private String username;
    
    @NotBlank(message = "密码不能为空")
    @Pattern(regexp = "^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d).{8,}$", 
             message = "密码必须包含大小写字母和数字，至少8位")
    private String password;
}

// Controller 中使用 @Valid
@PostMapping("/op/add")
public void add(@RequestBody @Valid UserAddDTO dto) {
    userService.add(dto);
}
```

### 6.2 敏感词过滤

系统自动过滤敏感词，配置在：
```yaml
akm:
  sensitiveWord: select|insert|update|delete|drop|...
```

## 七、日志规范

### 7.1 日志级别

```java
import lombok.extern.slf4j.Slf4j;

@Slf4j
@Service
public class UserService {
    public void processUser(String userId) {
        log.debug("开始处理用户: {}", userId);
        log.info("用户处理完成: {}", userId);
        log.warn("用户数据异常: {}", userId);
        log.error("用户处理失败: {}", userId, exception);
    }
}
```

### 7.2 日志配置

```yaml
logging:
  level:
    root: info
    com.akm.springboot3: info  # 可动态调整
  file:
    path: ./logs
```

## 八、安全规范

### 8.1 参数加解密

```yaml
akm:
  enabledEncrypt: true  # 启用加解密
  encryptExcludeUrls:   # 排除路径
    - /auth/open/config
```

### 8.2 接口签名

```yaml
akm:
  enabledSign: true     # 启用签名
  signTimeTolerance: 120  # 签名有效期(秒)
```

### 8.3 频率限制

```java
// 全局配置
akm:
  enabledApiFreq: true
  apiFreq: 600,100  # 10分钟内最多100次
  opApiFreq: 600,20 # 操作接口更严格

// 针对特定接口
@ApiFreqLimit(time = 60, limit = 10)
@GetMapping("/view/list")
public PageResult<User> list() {
    // ...
}
```

## 九、最佳实践

### 9.1 使用 Lombok

```java
@Getter
@Setter
@ToString
@NoArgsConstructor
@AllArgsConstructor
public class User {
    private String id;
    private String username;
}
```

### 9.2 使用 Java 17 特性

```java
// Record
public record UserInfo(String id, String username, String email) {}

// Switch 表达式
String message = switch (status) {
    case ACTIVE -> "用户活跃";
    case INACTIVE -> "用户非活跃";
    default -> "未知状态";
};

// Text Blocks
String sql = """
    SELECT * FROM sys_user
    WHERE username = ?
    AND status = 'ACTIVE'
    """;
```

### 9.3 构造器注入

```java
// ✅ 推荐
@Service
public class UserService {
    private final UserMapper userMapper;
    
    public UserService(UserMapper userMapper) {
        this.userMapper = userMapper;
    }
}

// ❌ 不推荐
@Service
public class UserService {
    @Autowired
    private UserMapper userMapper;
}
```

### 9.4 异步处理

```java
@Async
public void sendEmail(String email, String content) {
    // 异步发送邮件
}
```

## 十、代码审查清单

- [ ] API 路径符合规范（/view/, /op/, /open/, /public/）
- [ ] 使用构造器注入，不使用 @Autowired 字段注入
- [ ] 所有接口都有 Swagger 注解
- [ ] 参数校验使用 @Valid
- [ ] 缓存 Key 在 RedisKeys 中定义
- [ ] 敏感信息已加密
- [ ] SQL 使用 #{} 而不是 ${}
- [ ] 异常使用 BusinessException
- [ ] 日志级别正确
- [ ] 代码格式符合规范

## 十一、工具类使用

### Hutool
```java
// 字符串
StrUtil.isBlank(str);
StrUtil.format("Hello {}", name);

// 集合
CollUtil.isEmpty(list);
CollUtil.newArrayList(1, 2, 3);

// 日期
DateUtil.now();
DateUtil.format(date, "yyyy-MM-dd");

// JSON
JSONUtil.toJsonStr(object);
JSONUtil.toBean(json, User.class);
```

### 自定义工具类
```java
// 缓存
CacheUtils.get(key);
StringCacheUtils.set(key, value, timeout);

// 加解密
EncryptUtils.encrypt(data);
EncryptUtils.decrypt(data);

// 用户信息
UserThreadUtils.getUserId();
UserThreadUtils.getUsername();

// IP
IpUtils.getIpAddr(request);

// ID 生成
SnowflakeUtils.nextId();
```

## 十二、Git 提交规范

```bash
# 格式
[类型] 简短描述

# 类型
feat     - 新功能
fix      - 修复bug
docs     - 文档更新
style    - 代码格式
refactor - 重构
test     - 测试
chore    - 构建/工具

# 示例
git commit -m "[feat] 新增用户管理模块"
git commit -m "[fix] 修复用户查询分页问题"
git commit -m "[docs] 更新API文档"
```

## 十三、开发工具推荐

### 13.1 MyBatis XML 编写工具

手工编写 MyBatis XML 文件费时费力，强烈推荐使用以下工具：

#### IDEA 插件（推荐）

**1. MyBatisCodeHelper Pro**（付费，功能最强大）

**核心功能**：
- ✅ 根据 Mapper 方法自动生成 XML SQL
- ✅ Java 和 XML 之间快速跳转（Ctrl/Cmd + Click）
- ✅ SQL 语法检查和智能提示
- ✅ 字段名、表名自动补全
- ✅ 批量生成 CRUD 方法
- ✅ SQL 格式化
- ✅ 支持多种数据库

**安装**：
```
IDEA → Settings → Plugins → 搜索 "MyBatisCodeHelper Pro"
```

**使用示例**：
```java
// 1. 在 Mapper 接口中定义方法
public interface UserMapper {
    List<User> findByUsernameAndStatus(String username, String status);
}

// 2. 光标放在方法上，按 Alt + Enter
// 3. 选择 "Generate MyBatis SQL"
// 4. 插件自动生成 XML：

<select id="findByUsernameAndStatus" resultType="User">
    SELECT * FROM sys_user 
    WHERE username = #{username} 
    AND status = #{status}
</select>
```

**2. MyBatisX**（免费）

**核心功能**：
- ✅ Mapper 和 XML 之间跳转
- ✅ 自动生成代码
- ✅ SQL 语句检查
- ✅ 参数提示

**安装**：
```
IDEA → Settings → Plugins → 搜索 "MyBatisX"
```

**3. MyBatis Log Plugin**（免费）

**功能**：格式化控制台中的 MyBatis 日志，还原完整可执行的 SQL

**安装**：
```
IDEA → Settings → Plugins → 搜索 "MyBatis Log"
```

**使用**：
```
原始日志：
Preparing: SELECT * FROM sys_user WHERE id = ?
Parameters: 1(String)

格式化后（可直接执行）：
SELECT * FROM sys_user WHERE id = '1';
```

#### AI 工具辅助

**1. Cursor AI / GitHub Copilot**

直接在代码中使用注释提示 AI：

```java
public interface UserMapper {
    // 查询用户列表，支持用户名模糊查询和状态精确查询，按创建时间降序
    List<User> findByUsernameAndStatus(
        @Param("username") String username, 
        @Param("status") String status
    );
}
```

Cursor AI 会自动在 XML 中生成：
```xml
<select id="findByUsernameAndStatus" resultType="User">
    SELECT * FROM sys_user
    WHERE 1=1
    <if test="username != null and username != ''">
        AND username LIKE CONCAT('%', #{username}, '%')
    </if>
    <if test="status != null and status != ''">
        AND status = #{status}
    </if>
    ORDER BY create_time DESC
</select>
```

**2. ChatGPT / Claude**

使用提示词模板：

```
请帮我生成 MyBatis XML 查询语句：

表结构：
- 表名：sys_user
- 字段：id, username, password, email, status, create_time

需求：
1. 根据 username 模糊查询
2. 根据 status 精确匹配
3. 根据 create_time 范围查询
4. 所有条件都是可选的（使用 <if> 标签）
5. 按 create_time 降序排序
6. 支持分页（使用 PageHelper）

Mapper 接口：
List<SysUser> findByAll(@Param("query") SysUserQuery query);
```

AI 会生成完整的 XML：
```xml
<select id="findByAll" resultType="com.akm.springboot3.web.sys.entity.SysUser">
    SELECT * FROM sys_user
    WHERE del_flag = '0'
    <if test="query.username != null and query.username != ''">
        AND username LIKE CONCAT('%', #{query.username}, '%')
    </if>
    <if test="query.status != null and query.status != ''">
        AND status = #{query.status}
    </if>
    <if test="query.startTime != null">
        AND create_time &gt;= #{query.startTime}
    </if>
    <if test="query.endTime != null">
        AND create_time &lt;= #{query.endTime}
    </if>
    ORDER BY create_time DESC
</select>
```

#### MyBatis Generator

**官方代码生成工具**，根据数据库表自动生成 Entity、Mapper、XML

**1. 添加插件**：
```xml
<!-- pom.xml -->
<plugin>
    <groupId>org.mybatis.generator</groupId>
    <artifactId>mybatis-generator-maven-plugin</artifactId>
    <version>1.4.2</version>
    <configuration>
        <configurationFile>
            src/main/resources/generatorConfig.xml
        </configurationFile>
        <overwrite>true</overwrite>
    </configuration>
</plugin>
```

**2. 配置生成规则**：
```xml
<!-- src/main/resources/generatorConfig.xml -->
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration>
<generatorConfiguration>
    <context id="MySQL" targetRuntime="MyBatis3">
        <jdbcConnection 
            driverClass="com.mysql.cj.jdbc.Driver"
            connectionURL="jdbc:mysql://localhost:3306/akm_springboot"
            userId="root"
            password="password"/>
        
        <javaModelGenerator 
            targetPackage="com.akm.springboot3.web.sys.entity" 
            targetProject="src/main/java"/>
        
        <sqlMapGenerator 
            targetPackage="mapper.sys" 
            targetProject="src/main/resources"/>
        
        <javaClientGenerator 
            type="XMLMAPPER" 
            targetPackage="com.akm.springboot3.web.sys.mapper" 
            targetProject="src/main/java"/>
        
        <table tableName="sys_user" 
               domainObjectName="SysUser"
               enableCountByExample="false"
               enableUpdateByExample="false"
               enableDeleteByExample="false"
               enableSelectByExample="false"/>
    </context>
</generatorConfiguration>
```

**3. 执行生成**：
```bash
mvn mybatis-generator:generate
```

**生成结果**：
- Entity: `SysUser.java`
- Mapper: `SysUserMapper.java`
- XML: `SysUserMapper.xml`（包含基础 CRUD）

### 13.2 数据库管理工具

| 工具 | 说明 | 推荐指数 |
|------|------|---------|
| **Navicat** | 功能强大，支持多种数据库 | ⭐⭐⭐⭐⭐ |
| **DataGrip** | JetBrains 出品，IDEA 集成 | ⭐⭐⭐⭐⭐ |
| **DBeaver** | 免费开源，跨平台 | ⭐⭐⭐⭐ |
| **MySQL Workbench** | MySQL 官方工具 | ⭐⭐⭐ |

### 13.3 API 测试工具

| 工具 | 说明 | 推荐指数 |
|------|------|---------|
| **Postman** | 功能最全面 | ⭐⭐⭐⭐⭐ |
| **Swagger UI** | 项目自带，开箱即用 | ⭐⭐⭐⭐⭐ |
| **Apifox** | 国产工具，支持中文 | ⭐⭐⭐⭐ |
| **curl** | 命令行工具 | ⭐⭐⭐ |

### 13.4 代码质量工具

| 工具 | 说明 |
|------|------|
| **SonarLint** | IDEA 插件，实时代码质量检查 |
| **Alibaba Java Coding Guidelines** | 阿里巴巴 Java 开发规约插件 |
| **CheckStyle** | 代码风格检查 |

### 13.5 Git 工具

| 工具 | 说明 |
|------|------|
| **IDEA Git** | IDEA 内置 Git 工具 |
| **SourceTree** | 可视化 Git 客户端 |
| **GitKraken** | 美观的 Git 客户端 |

## 🐛 调试技巧

